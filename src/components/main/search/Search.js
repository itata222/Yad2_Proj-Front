import React from "react";

const Search = () => {
    return (
        <div className="search">
            <form className="searchForm">
                <input type="text" />
            </form>
        </div>
    );
};

export default Search;
